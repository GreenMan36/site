# 05 — Low priority (batch when touching the area)

- TD-050 [LOW] pages/Commissies.vue:2-4,14-20 vs pages/Dispuut.vue:2,8-13 — same collection-card skeleton (ordered query + ContentCard `#image` slot + ContentRenderer). → One shared card-list component/pattern for collection-card pages. Impact S / Effort S → acceptance: - [ ] skeleton edited once
- TD-051 [LOW] components/JobOffers.vue:5,13-16,24,29-31 — accordion toggles `open` via `classList` on `parentElement` from h3 click (invisible to Vue, lost on re-render), no `aria-expanded`, `display:none`-only hiding, `body?:any`, inline template styles. → Reactive open-set or native `<details>/<summary>`. Impact S / Effort S → acceptance: - [ ] state in Vue; expanded announced; no `any`
- TD-052 [LOW] content/boards/2024-2025.md:9,14,19,24,29 (member photos `b8-*` missing under `public/assets/boards/`; only group photo exists) + `2021-2022.md:5` (`groupPhoto: ""` instead of omitted) + archived years' photo/figcaption/email fields authored but never rendered (`besturen.vue:41-47` lists names only; photos render for current year only). → Restore the five images or prune per-member photo fields to current-year-only. Impact S / Effort S → acceptance: - [ ] no dangling photo refs; archived schema minimal
- TD-053 [LOW] pages/Intro.vue:4,31-52 — fully hardcoded seasonal marketing page (2026 dates, `forms.gle` links, `intro2023lowres.webp` asset) bypassing the content system; editors need a code PR for dates/links. → Migrate into a content collection (home MDC pattern: `content/index.md` + ContentRenderer) or accept as code-owned seasonal page and refresh the asset. Impact S / Effort M → acceptance: - [ ] editors update dates/links without a PR, or page marked code-owned with fresh assets
- TD-054 [LOW] pages/Activiteiten.vue:1-26 — no inbound links (nav/footer/links/content/components greps empty; prerender crawl never includes it). → Delete, or link it from nav/footer if it's meant to be found. Impact S / Effort S → acceptance: - [x] page reachable or removed — DONE 2026-09-13: page deleted (and its only consumer of AGENDA_EMBED_URL). It was prerendered but unlinked; the homepage calendar covers the agenda. NOTE: /activiteiten now falls through to the 404 catch-all, so any external link to it breaks.
- TD-055 [LOW] assets/css batch — dead tokens (`--header-font`, `--filter`, `--sidebar-width`, `--inner-sidebar-*`, `--brand-navy-1/2/4`, `--brand-orange-hover`, 13 `--cq-*` tokens with zero consumers; doc comment `variables.css:69-72` + skill docs recommend `@container (min-width: var(--cq-md))`, which browsers reject — real code correctly uses literals) + dead `main.css:151-155` no-op media query + font contradiction (`main.css` hardcodes Roboto, `typography.css:10-12,24-25` hardcode Indicium-font, `--text-font` token says Calibri, only `LinkCard.vue:39` opts in) + `typography.css:1-33` group rule re-declared per level + identical h2 accent bar in `HomeTextBlock.vue:26-35`, `over-indicium.vue:54-55`, `Bestuur.vue:96-106`. → Delete unused tokens, fix the `@container` example to rem literals, remove dead query, pick one body font via tokens, slim per-level rules, promote accent bar to one shared rule. Impact S / Effort S → acceptance: - [ ] no dead tokens; fonts consistent; one accent-bar source
- TD-056 [LOW] nuxt.config.ts:12-33 (inline module re-applying @nuxt/content's optimizeDeps rewrite) — dev startup logs NUXT_B7002 for ten `@nuxtjs/mdc > <dep>` entries: the @nuxtjs/mdc module pushes them and @nuxt/content's shim for exactly that id shape (`@nuxt/content/dist/module.mjs:2721`) does not catch them, so they reach Vite unscoped; Vite resolves `pkg > dep` with `basedir = resolvePackageData(pkg, rootDir)?.dir || rootDir` (`vite/dist/node/chunks/node.js`: createOptimizeDepsIncludeResolver + nestedResolveBasedir) and @nuxtjs/mdc is not a project dependency, so all ten fail against the root fallback basedir. → Keep the late rewrite (verified A/B/A in an isolated probe with the same dependency shape: warning present → absent → present); delete when upstream orders its shim after mdc's push, or if @nuxtjs/mdc becomes a root dependency. Impact S / Effort S → acceptance: - [x] no NUXT_B7002 on dev startup; - [ ] workaround deleted once upstream fixes ordering
- TD-057 [LOW] package.json (`"nuxt-studio": "https://pkg.pr.new/nuxt-studio@6a91693"`) — Studio is pinned to an unreleased `main`-branch preview build (commit 2026-09-03): the released 1.7.0 has no nested drag handles (upstream PR #490) and the preview also swapped the document pipeline (remark-mdc → comark). No upstream support, nothing to diff a release against, and no automated check that the assumptions this repo now depends on still hold. → Return to a released `nuxt-studio` once one contains #490; until then follow the "Studio upgrade checklist" in `.agents/skills/indicium-content/references/nuxt-studio.md`. Impact M / Effort S → acceptance: - [ ] a release carries the nested drag handles; - [ ] the preview URL is gone from package.json
- TD-058 [LOW] public/assets/images/** + public/*.jpg — the 2026-09-08 perf baseline flagged `intro2023.jpg` (6.5MB) as referenced nowhere while its `.webp` twin is used, and noted the same pattern likely applies to other unreferenced twins; total dead weight is plausibly >10MB. Nothing has been pruned because `public/` paths are also pinned outside the repo (mailchimp emails — see `public/logo/README.md`), so deletion needs an owner check. → Audit public/ image references, confirm nothing is hotlinked from emails/socials, then delete the unreferenced originals. Impact M / Effort S → acceptance: - [ ] no unreferenced heavyweight binaries in public/; - [ ] no externally pinned path broken
